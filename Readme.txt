To run the application please use the command => node app.js
To run test cases run the command => npm test

We are following the top down approach for printing the hierarchy

We first find the top layer of employees and recursively find employees below them.
So we only get the valid employees.

